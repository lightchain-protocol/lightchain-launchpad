node_modules/
dist/
.env
.env.local
*.tsbuildinfo

# next
.next/
out/

# ponder
.ponder/
generated/

# hardhat
cache/
artifacts/
typechain-types/

# misc
.DS_Store
*.log
coverage/
package-lock.json
# …but contracts/ is outside the pnpm workspace and installs with npm.
# `npm ci` refuses to run without a lockfile, so this one must be tracked.
!contracts/package-lock.json
yarn.lock


# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next/
out/

# production
build
dist

# turbo
.turbo

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# …but the templates must ship, contributors need them
!.env.example
!**/.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# agent / editor config
CLAUDE.md
.claude/
.cursor/
.agents/
plans/
